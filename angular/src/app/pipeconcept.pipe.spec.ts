import { PipeconceptPipe } from './pipeconcept.pipe';

describe('PipeconceptPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeconceptPipe();
    expect(pipe).toBeTruthy();
  });
});
